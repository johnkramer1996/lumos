import { mockTypes } from './types'

const initialState = {}

export default function mockReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}
